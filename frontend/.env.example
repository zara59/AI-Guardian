# Base URL of the AI Guardian backend (Phase 2).
VITE_API_BASE_URL=http://localhost:3000

# RPC for the Sepolia Guardian deployment (Phase 4 wallet execution).
# Optional — a public endpoint is used when unset.
VITE_SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com